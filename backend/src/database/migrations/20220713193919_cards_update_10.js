import { CARD_FIELDS_ADDITION_10 } from '../cardFields';
import { addFieldsToCards } from '../migrationsHelper';

// eslint-disable-next-line import/no-commonjs
module.exports = addFieldsToCards(CARD_FIELDS_ADDITION_10);
