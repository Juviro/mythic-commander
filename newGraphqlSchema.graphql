type Card {
    id: String!
    name: String!

    imgKey: String!
    possiblePartners: String!
}







deck {
    id
    name

    cards {
        id: deckId + cardId
        amount

        coreFields {
            id
            oracle_id
            name

            owned
            amountOwned ? maybe use amount here as well ?
            
        }
    }
}