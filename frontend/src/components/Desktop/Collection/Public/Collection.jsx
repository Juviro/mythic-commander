// import React from 'react';
// import { useQuery } from '@apollo/react-hooks';

// import { getPublicCollectionDesktop } from './queries';

export default () => {
  // const { data, loading } = useQuery(getPublicCollectionDesktop);
  return null;
};
