{
  "name": "mtg",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "NODE_ENV=dev concurrently \"cd ./backend && yarn start\" \"cd ./frontend && yarn start\"",
    "startDb": "brew services start postgresql",
    "stopDb": "brew services stop postgresql",
    "deploy": "(cd /home/hauke/packages/mtg/frontend && yarn install --production=false && yarn build && rm -rf /var/www/virtual/hauke/html/* && cp -r /home/hauke/packages/mtg/frontend/build/* /var/www/virtual/hauke/html/ && cd /home/hauke/packages/mtg/backend && yarn install --production=false && yarn build && supervisorctl restart mtg)"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "prettier": "^1.19.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
